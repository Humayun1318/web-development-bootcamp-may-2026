{
  "name": "personal-expense-tracker-backend",
  "version": "1.0.0",
  "description": "Express + MongoDB backend",
  "main": "dist/server.js",
  "author": "Humayun Kabir",
  "license": "MIT",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --no-notify --ignore-watch node_modules src/server.ts",
    "start": "node dist/server.js",
    "build": "npm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint --ext .ts,.js .",
    "format": "prettier --write \"src/**/*.{ts,js,json,md}\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.2",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.20.0",
    "@typescript-eslint/parser": "^7.20.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.0",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}